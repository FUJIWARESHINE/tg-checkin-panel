# 更新日志

本项目的所有重要变更都会记录在此文件。
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.1] - 2026-09-14

### 修复

- **二维码登录接口报错**：`QRLogin.expires` 是「过期时间点」（datetime）而不是剩余秒数，
  旧写法把它塞进 `timedelta(seconds=...)` 会抛 `TypeError: unsupported type for timedelta seconds component`，
  导致二维码登录直接 500。现在统一换算成带时区的 UTC 时间点返回。
- **点击按钮步骤报错**：Telethon 1.37 起 `Message.click()` 已移除 `timeout` 参数，
  旧调用会抛 `TypeError: Message.click() got an unexpected keyword argument 'timeout'`，
  凡是动作流里带「点击按钮」的任务都会失败（`ai_choose` 的自动点击同样修正）。
  改为在外层用 `asyncio.wait_for` 控制超时，语义与原来一致。
- **等待回复串台**：`wait_reply` 会命中「上一条指令」的延迟回复（同一会话配了多个目标时尤其明显），
  把上一次的回复当成本次的签到结果。现在以本次发送的消息 id 为基线，丢弃比它更旧的来消息，
  已排队的历史消息也会在发送后清理。
- **点击按钮静默失败**：消息上没有按钮、按钮文字对不上、序号越界时，Telethon 会静默返回 `None`，
  面板却记录成「已点击按钮」，真正的失败被推迟成后面那条莫名的「等待回复超时」。
  现在这些情况会直接报错，并在错误信息里列出可点击的按钮；按钮文字改为不区分大小写的
  **包含匹配**（带 emoji 的按钮如「🗓 每日签到」也能点中）；按钮确实点到、但机器人没有返回应答时
  不判失败，仅在日志与步骤结果里明确提示。

## [1.0.0] - 2026-09-13

首个正式版本。

### 新增

- **多账号管理**：手机号 / 验证码 / 两步验证 / 二维码 四种登录方式，Session 加密落盘
- **可视化动作流编辑器**：拖拽卡片编排签到流程（8 种步骤），无需手写 JSON
- **灵活调度**：Cron（5/6 字段）、每天定点、固定间隔；随机延迟、错过补跑、失败重试
- **结果判定**：按关键词 / 正则判断签到是否真正成功，支持从回复中提取积分
- **精美 Web 界面**：Vue 3 + Naive UI，亮/暗双主题，仪表盘与 ECharts 趋势图，WebSocket 实时日志
- **多渠道推送**：Telegram Bot / Server酱 / Bark / 企业微信 / 钉钉 / 飞书 / Gotify / Webhook / 邮件，含静默时段与连续失败合并降噪
- **安全**：管理员登录 + 登录限流、敏感字段 Fernet 加密、日志脱敏
- **AI 识图答题**：预留接口（可选，需 OpenAI 兼容 Key）

### 工程

- 单容器部署（多阶段构建，前端由 FastAPI 托管，无需 Nginx）
- GitHub Actions 自动构建多架构镜像（linux/amd64、linux/arm64）并发布到 GHCR
- 推送版本 tag 自动创建 Release，附自动生成的变更说明
- 后端冒烟测试 61 项，CI 中作为镜像构建的前置关卡
